const auth = require('../../src/middleware/auth')

describe('Authorization', () => {

    const user = null
    const teacher = null
    const admin = null

    it.todo('allows user to solve tests')
    it.todo('denies user to access admin panel')

    it.todo('allows teacher to create tests')
    it.todo('denies teacher to access admin panel')

    it.todo('allows admin to access admin panel')
})